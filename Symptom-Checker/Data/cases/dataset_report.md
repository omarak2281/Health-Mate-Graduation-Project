# Structured Dataset Generation Report (Phase 3)

Generated by `data_pipeline/generate_structured_cases.py`, seed=42.

- Total cases: **12300**
- Diseases covered: **41** (target 41)
- Cases per disease: fixed at **300** (uniform by construction, well above the plan's >=250/class floor)

## Urgency label distribution (computed via the Phase 2 rule engine, not invented)

- low: 1981
- moderate: 4150
- high: 2342
- critical: 3827

## Risk-factor vocabulary gap (flagged, see plan §4.5 vs Phase 1 output)

Plan §4.5 says `N_risk_factors` is derived from the migrated taxonomy, but Phase 1's taxonomy schema (categories/symptoms/diseases) never defines a risk-factor list. Used the plan's own two literal examples (`"hypertension"`, `"asthma"`) as the seed and extended to a small pool of already-migrated chronic disease ids: `['hypertension', 'diabetes_mellitus_type_2', 'asthma', 'hypothyroidism', 'chronic_kidney_disease']`. This needs product/clinical review before Phase 4 treats it as final.

## Known limitation: urgency skew for red-flag-heavy diseases

Diseases whose `related_symptom_ids` include one or more red-flag symptoms (e.g. `chest_pain`, `shortness_of_breath`) end up with a higher share of `high`/`critical` labels than their `default_urgency` alone would suggest, because the rule engine escalates urgency whenever a sampled red-flag symptom lands at severity>=2. This is clinically defensible per-case (a red-flag symptom at meaningful severity IS urgent) but the aggregate distribution below may not match true epidemiological prevalence for these diagnoses (most real-world Hypertension presentations are not emergencies). Flagged for clinical/product review before Phase 4 trains on this as ground truth — not silently shipped as-is.

- `aortic_stenosis`: 98% of cases labeled high/critical
- `ventricular_tachycardia`: 96% of cases labeled high/critical
- `hypertrophic_cardiomyopathy`: 95% of cases labeled high/critical
- `myocarditis`: 92% of cases labeled high/critical
- `stable_angina`: 92% of cases labeled high/critical
- `aortic_aneurysm`: 91% of cases labeled high/critical
- `hypertension`: 90% of cases labeled high/critical
- `pericarditis`: 90% of cases labeled high/critical
- `coronary_artery_disease`: 87% of cases labeled high/critical
- `stroke`: 86% of cases labeled high/critical
- `pulmonary_hypertension`: 86% of cases labeled high/critical
- `pneumonia`: 82% of cases labeled high/critical
- `atrial_septal_defect_asd`: 81% of cases labeled high/critical
- `congestive_heart_failure`: 80% of cases labeled high/critical
- `dilated_cardiomyopathy`: 79% of cases labeled high/critical
- `heart_failure`: 79% of cases labeled high/critical
- `mitral_regurgitation`: 77% of cases labeled high/critical
- `atrial_fibrillation`: 71% of cases labeled high/critical

## Confusable-pair differentiation applied

- `common_cold` / `influenza` / `covid_19`: distinct duration_days ranges ({'common_cold': (2, 5), 'influenza': (3, 7), 'covid_19': (5, 14)}), distinct severity ranges ({'common_cold': (1, 2), 'influenza': (2, 4), 'covid_19': (1, 3)}), and `covid_19` has `loss_of_taste_smell` boosted to 85% inclusion probability as its pathognomonic differentiator.
- `hypertension` vs `migraine` / `stroke`: checked directly against `taxonomy/diseases.json` — these diseases' `related_symptom_ids` sets do not overlap at all, so they are already naturally separated by symptom identity; `hypertension` additionally carries elevated vitals in 75% of its cases (vs a 10% background rate elsewhere), which `migraine`/`stroke` never receive.
