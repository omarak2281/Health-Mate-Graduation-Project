# Taxonomy Migration Report (Phase 1)

Generated by `migration/migrate_legacy_taxonomy.py`. Source of truth: the 4 legacy `.json` files under `Data/General` and `Data/Heart` (`.js` variants ignored per the plan).

## Counts

- Diseases migrated: **41** (General 23 + Heart 18)
- Symptoms defined in source files: 34 (General) + 18 (Heart) = 52 raw entries
- Symptoms after dedup of directly-defined entries: **49**
- Orphan stub symptoms created (referenced by a disease but never defined as their own entry): **59**
- Total symptoms in migrated taxonomy: **108**
- Categories: **9** (fixed set per plan §4.1)

## Deviation from the plan's §1.2 overlap claim

The plan states the General/Heart symptom files overlap on `Chest Pain`, `Shortness of Breath`, `Fatigue`, `Dizziness`. Actual inspection of the JSON keys (not `relatedSymptoms`/`relatedDiseases` values, the top-level symptom entries themselves) shows the overlapping **keys** are `Chest Pain`, `Shortness of Breath`, and `Fever` (3 dedup merge(s) performed — see below). `Fatigue` and `Dizziness` are only defined in `GeneralSymptoms.json`; `HeartSymptoms.json` references them inside disease `relatedSymptoms` lists but never defines them as its own top-level symptom entry, so there was nothing to deduplicate for those two names. This has been written back into `Plans/SYMPTOM_CHECKER_IMPLEMENTATION_PLAN.md` §1.2 per the plan's own handoff instruction #5 (deviations must be recorded in the plan document).

## Symptom key dedup log (General/Heart direct overlaps)

- `chest_pain` — merged duplicate definition from **heart** (already defined via **general**)
- `shortness_of_breath` — merged duplicate definition from **heart** (already defined via **general**)
- `fever` — merged duplicate definition from **heart** (already defined via **general**)

### Description conflicts on dedup (kept General's text, Heart's discarded)

- `chest_pain`: kept **general** description ("Pain or discomfort in chest area"); discarded **heart** variant ("Pain or discomfort in the chest area that may indicate heart problems") — logged here for manual review, no data lost.
- `fever`: kept **general** description ("Temporary increase in body temperature, often due to illness"); discarded **heart** variant ("Elevated body temperature above normal range") — logged here for manual review, no data lost.

## Category mapping table (old free-text `category` -> new category id)

| Old category | New category id |
|---|---|
| Respiratory Infection | `respiratory` |
| Chronic Respiratory | `respiratory` |
| Gastrointestinal | `digestive` |
| Metabolic | `general_fever` |
| Endocrine | `general_fever` |
| Musculoskeletal | `muscles_joints` |
| Neurological | `neurological` |
| Psychiatric | `mental_health` |
| Dermatological | `skin` |
| Infectious Disease | `respiratory` |
| Hematological | `general_fever` |
| Liver Disease | `digestive` |
| Renal | `urinary_kidney` |
| Cardiovascular | `heart_bp` |
| Coronary Artery Disease | `heart_bp` |
| Heart Failure | `heart_bp` |
| Valvular Heart Disease | `heart_bp` |
| Cardiomyopathy | `heart_bp` |
| Arrhythmia | `heart_bp` |
| Inflammatory Heart Disease | `heart_bp` |
| Vascular Disease | `heart_bp` |
| Congenital Heart Defect | `heart_bp` |

Ambiguous best-fit decisions (no dedicated category exists in the fixed 9-set): `Metabolic` -> `general_fever`, `Endocrine` -> `general_fever`, `Hematological` -> `general_fever`, `Liver Disease` -> `digestive`, `Infectious Disease` -> `respiratory` (both diseases in that bucket — COVID-19, Tuberculosis — are primarily respiratory). Flagged per plan §6 Phase 1 instruction to log rather than silently guess.

## Orphan symptom stubs (59)

Symptom names referenced in a disease's `relatedSymptoms` array that have no matching top-level entry in either `GeneralSymptoms.json` or `HeartSymptoms.json`. This is a data gap in the legacy source, not introduced by migration. Each was kept as a stub (Arabic translation and description left empty, `needs_translation=true`) rather than silently dropping the disease-symptom relationship. **Action required before Phase 3**: a human should review this list, supply `name_ar`/descriptions, and decide whether any stub is actually a duplicate of an existing symptom under different wording (e.g. `swelling_in_legs` vs. `edema`/`peripheral_edema` — several candidates below look related but were NOT merged automatically, per the plan's instruction not to guess medical equivalence).

**Exception (4 of 59, added during post-Phase-12 review):** the orphan stubs that are also plan-mandated red-flag symptoms (`sudden_numbness`, `trouble_speaking`, `loss_of_balance`, `loss_of_consciousness` — the classic FAST stroke warning signs) were manually translated via `MANUAL_STUB_TRANSLATIONS` and no longer carry `needs_translation=true`, because shipping the app's most safety-critical symptom set with zero Arabic text was judged too risky to leave for a later, unscheduled clinical-review pass. The remaining stubs are unaffected and still need the review described above.

- `bloating` ("Bloating") — referenced by **Irritable Bowel Syndrome (IBS)** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `gas` ("Gas") — referenced by **Irritable Bowel Syndrome (IBS)** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `constipation` ("Constipation") — referenced by **Irritable Bowel Syndrome (IBS)** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `increased_thirst` ("Increased Thirst") — referenced by **Diabetes Mellitus Type 2** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `increased_hunger` ("Increased Hunger") — referenced by **Diabetes Mellitus Type 2** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `blurred_vision` ("Blurred Vision") — referenced by **Diabetes Mellitus Type 2** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `cold_intolerance` ("Cold Intolerance") — referenced by **Hypothyroidism** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `dry_skin` ("Dry Skin") — referenced by **Hypothyroidism** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `stiffness` ("Stiffness") — referenced by **Osteoarthritis** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `tenderness` ("Tenderness") — referenced by **Osteoarthritis** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `loss_of_flexibility` ("Loss of Flexibility") — referenced by **Osteoarthritis** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `swelling` ("Swelling") — referenced by **Osteoarthritis** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `joint_swelling` ("Joint Swelling") — referenced by **Rheumatoid Arthritis** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `joint_stiffness` ("Joint Stiffness") — referenced by **Rheumatoid Arthritis** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `throbbing_head_pain` ("Throbbing Head Pain") — referenced by **Migraine** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `sensitivity_to_light` ("Sensitivity to Light") — referenced by **Migraine** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `sensitivity_to_sound` ("Sensitivity to Sound") — referenced by **Migraine** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `seizures` ("Seizures") — referenced by **Epilepsy** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `temporary_confusion` ("Temporary Confusion") — referenced by **Epilepsy** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `staring_spells` ("Staring Spells") — referenced by **Epilepsy** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `loss_of_consciousness` ("Loss of Consciousness") — referenced by **Epilepsy** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom, but manually translated (`needs_translation=false`) because it is also one of the plan-mandated red-flag symptoms — see MANUAL_STUB_TRANSLATIONS.
- `persistent_sadness` ("Persistent Sadness") — referenced by **Major Depressive Disorder** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `loss_of_interest` ("Loss of Interest") — referenced by **Major Depressive Disorder** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `changes_in_appetite` ("Changes in Appetite") — referenced by **Major Depressive Disorder** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `sleep_problems` ("Sleep Problems") — referenced by **Major Depressive Disorder** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `feelings_of_worthlessness` ("Feelings of Worthlessness") — referenced by **Major Depressive Disorder** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `intense_itching` ("Intense Itching") — referenced by **Eczema** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `red_patches` ("Red Patches") — referenced by **Eczema** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `cracked_skin` ("Cracked Skin") — referenced by **Eczema** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `loss_of_taste_smell` ("Loss of Taste/Smell") — referenced by **COVID-19** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `persistent_cough` ("Persistent Cough") — referenced by **Tuberculosis** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `coughing_up_blood` ("Coughing up Blood") — referenced by **Tuberculosis** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `unintentional_weight_loss` ("Unintentional Weight Loss") — referenced by **Tuberculosis** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `night_sweats` ("Night Sweats") — referenced by **Tuberculosis** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `weakness` ("Weakness") — referenced by **Anemia** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `pale_skin` ("Pale Skin") — referenced by **Anemia** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `swelling_in_legs` ("Swelling in Legs") — referenced by **Liver Cirrhosis** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `ascites` ("Ascites") — referenced by **Liver Cirrhosis** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `swelling_in_ankles` ("Swelling in Ankles") — referenced by **Chronic Kidney Disease** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `severe_flank_pain` ("Severe Flank Pain") — referenced by **Kidney Stones** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `pain_on_urination` ("Pain on Urination") — referenced by **Kidney Stones** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `pink_red_or_brown_urine` ("Pink, Red or Brown Urine") — referenced by **Kidney Stones** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `sudden_numbness` ("Sudden Numbness") — referenced by **Stroke** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom, but manually translated (`needs_translation=false`) because it is also one of the plan-mandated red-flag symptoms — see MANUAL_STUB_TRANSLATIONS.
- `confusion` ("Confusion") — referenced by **Stroke** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `trouble_speaking` ("Trouble Speaking") — referenced by **Stroke** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom, but manually translated (`needs_translation=false`) because it is also one of the plan-mandated red-flag symptoms — see MANUAL_STUB_TRANSLATIONS.
- `vision_problems` ("Vision Problems") — referenced by **Stroke** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `loss_of_balance` ("Loss of Balance") — referenced by **Stroke** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom, but manually translated (`needs_translation=false`) because it is also one of the plan-mandated red-flag symptoms — see MANUAL_STUB_TRANSLATIONS.
- `nosebleeds` ("Nosebleeds") — referenced by **Hypertension** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `radiating_pain_to_left_arm` ("Radiating Pain to Left Arm") — referenced by **Acute Myocardial Infarction** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `profuse_sweating` ("Profuse Sweating") — referenced by **Acute Myocardial Infarction** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `sense_of_impending_doom` ("Sense of Impending Doom") — referenced by **Acute Myocardial Infarction** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `pink_frothy_sputum` ("Pink Frothy Sputum") — referenced by **Congestive Heart Failure** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `reduced_exercise_tolerance` ("Reduced Exercise Tolerance") — referenced by **Dilated Cardiomyopathy** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `irregular_heartbeat` ("Irregular Heartbeat") — referenced by **Atrial Fibrillation** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `anxiety` ("Anxiety") — referenced by **Atrial Fibrillation** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `leg_pain` ("Leg Pain") — referenced by **Deep Vein Thrombosis (DVT)** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `warmth_over_area` ("Warmth over Area") — referenced by **Deep Vein Thrombosis (DVT)** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `redness_of_skin` ("Redness of Skin") — referenced by **Deep Vein Thrombosis (DVT)** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.
- `difficulty_swallowing` ("Difficulty Swallowing") — referenced by **Aortic Aneurysm** but has no own entry in GeneralSymptoms.json/HeartSymptoms.json. Created as a stub symptom (no `name_ar`, `needs_translation=true`) so the disease-symptom link is not silently dropped. Needs manual translation + clinical review before Phase 3.

## One manual spelling-alias applied

- `"Coughing"` -> `cough` (same concept as the already-defined `Cough` symptom; used by Asthma).

## Red-flag resolution (plan §4.2 minimum set + one logged extension)

- `chest_pain` ("Chest Pain") — found, `red_flag=true` set.
- `shortness_of_breath` ("Shortness of Breath") — found, `red_flag=true` set.
- `sudden_numbness` ("Sudden Numbness") — found, `red_flag=true` set.
- `trouble_speaking` ("Trouble Speaking") — found, `red_flag=true` set.
- `loss_of_balance` ("Loss of Balance") — found, `red_flag=true` set.
- `syncope` ("Syncope") — found, `red_flag=true` set.
- `loss_of_consciousness` ("Loss of Consciousness") — found, `red_flag=true` set.
- `sudden_loss_of_consciousness` ("Sudden Loss of Consciousness") — found, `red_flag=true` set.

Extension: `sudden_loss_of_consciousness` (from HeartSymptoms.json's own "Sudden Loss of Consciousness" entry, severity=critical, category=Emergency) was also flagged `red_flag=true` in addition to the literal `loss_of_consciousness` stub, since it is clearly the same clinical concept and already carried critical/Emergency metadata in the source data.

No sudden/severe headache variant beyond the base set was found in the migrated data (headache-related ids present: headache, mild_headache, throbbing_head_pain) — none of these represent a "sudden/severe" onset variant, so none were marked as a red flag. This is a decision, not an omission — flagged per plan §9.3.

## Symptoms with empty `category_ids` (12)

These symptoms are defined but never appear in any disease's `related_symptom_ids`, so no category could be derived for them. They will not surface when filtering the wizard's Step 2 by category until either a disease references them or they get a manual category assignment.

- `angina_pectoris` ("Angina Pectoris")
- `cardiac_arrest` ("Cardiac Arrest")
- `claudication` ("Claudication")
- `edema` ("Edema")
- `flank_pain` ("Flank Pain")
- `insomnia` ("Insomnia")
- `jugular_venous_distension` ("Jugular Venous Distension")
- `orthopnea` ("Orthopnea")
- `paroxysmal_nocturnal_dyspnea` ("Paroxysmal Nocturnal Dyspnea")
- `skin_rash` ("Skin Rash")
- `sudden_loss_of_consciousness` ("Sudden Loss of Consciousness")
- `tachycardia` ("Tachycardia")

## Disease-level `severity` -> `default_urgency`

Copied verbatim (not recomputed) per plan §1.3/§4.3's field-meaning clarification. The old per-symptom `severity` free-text field (a static per-symptom label, distinct from both `default_urgency` and the new dynamic per-assessment severity) is intentionally **not** migrated — the new `Symptom` schema has no equivalent field; it is superseded by the `red_flag` boolean plus the dynamic 0-4 severity entered per assessment.

## DoD check against plan §6 Phase 1

- `taxonomy/categories.json`: 9 entries (target: 9) — OK
- `taxonomy/diseases.json`: 41 entries (target: 41) — OK
- `taxonomy/symptoms.json`: 49 directly-defined entries after dedup (plan expected ~48-52 from merging 4 known overlaps; actual overlap was 3 keys, see deviation note above, so the directly-defined count lands close to but not identical to that estimate) + 59 orphan stubs = 108 total.
- All entries validate against `data_pipeline/schema.py` Pydantic models — OK (script would have raised otherwise).